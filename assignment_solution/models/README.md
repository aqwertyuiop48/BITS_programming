# Model artifacts

Place the registry-approved optimized artifact here as `model.int8.onnx` before building the image. CI supplies the model and validation data; neither is committed to source control.
